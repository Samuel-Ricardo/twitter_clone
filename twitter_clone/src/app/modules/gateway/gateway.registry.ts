import { AxiosGatewayRegistry as AXIOS } from './axios/axios.registry';
import { REACTIVE_GATEWAY_REGISTRY as REACTIVE } from './reactive/reactive.registry';

export const GatewayRegistry = {
  AXIOS,
  REACTIVE,
};
