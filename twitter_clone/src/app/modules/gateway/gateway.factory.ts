import { AxiosGatewayFactory as AXIOS } from './axios/axios.factory';
import { REACTIVE_GATEWAY_FACTORY as REACTIVE } from './reactive/reactive.factory';

export const GatewayFactory = {
  AXIOS,
  REACTIVE,
};
